# GonoPBX Environment Configuration
# Copy this file to .env and adjust the values
# Or run install.sh to generate .env automatically

# External IP address of this server (for SIP NAT traversal)
EXTERNAL_IP=127.0.0.1

# Admin login password (for the web GUI)
ADMIN_PASSWORD=changeme

# JWT secret for session tokens (use a long random string)
JWT_SECRET=change-me-in-production

# PostgreSQL database password
DB_PASSWORD=changeme

# Asterisk Manager Interface (AMI) password
AMI_PASSWORD=changeme

# Bind address for web GUI and API
# 0.0.0.0  = accessible from LAN (direct access, no reverse proxy)
# 127.0.0.1 = localhost only (use with reverse proxy like Nginx)
BIND_ADDRESS=0.0.0.0
